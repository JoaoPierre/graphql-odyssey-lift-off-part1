const resolvers = {
    Query: {
        // returns an array of track objects that are displayed on the
        // homepage grid

        tracksForHome: () => {

        }
    }
}

module.exports = resolvers;